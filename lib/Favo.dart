import 'package:flutter/material.dart';
class Favori extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(Icons.favorite,size: 50.0,),
    );
  }
}
